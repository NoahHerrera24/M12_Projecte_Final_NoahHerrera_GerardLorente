import { RankingTorneigListFilterPipe } from './ranking-torneig-list-filter.pipe';

describe('RankingTorneigListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RankingTorneigListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
