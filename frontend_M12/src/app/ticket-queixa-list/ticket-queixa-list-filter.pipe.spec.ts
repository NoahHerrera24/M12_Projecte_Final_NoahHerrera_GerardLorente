import { TicketQueixaListFilterPipe } from './ticket-queixa-list-filter.pipe';

describe('TicketQueixaListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketQueixaListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
