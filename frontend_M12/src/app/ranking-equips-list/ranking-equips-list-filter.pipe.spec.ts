import { RankingEquipsListFilterPipe } from './ranking-equips-list-filter.pipe';

describe('RankingEquipsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RankingEquipsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
