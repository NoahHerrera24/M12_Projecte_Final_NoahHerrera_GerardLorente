import { TorneigListFilterPipe } from './torneig-list-filter.pipe';

describe('TorneigListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TorneigListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
