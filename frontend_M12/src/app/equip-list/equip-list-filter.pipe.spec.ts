import { EquipListFilterPipe } from './equip-list-filter.pipe';

describe('EquipListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
